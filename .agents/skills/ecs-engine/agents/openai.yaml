interface:
  display_name: "Ecs Engine"
  short_description: "Repo-specific patterns and workflows for ecs-engine"
  default_prompt: "Use the ecs-engine repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true